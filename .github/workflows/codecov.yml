name: CodeCov Test and coverage

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: codecov/codecov-action@v2
      with:
	token: 764eb126-04d6-46cf-a952-cb51766c8572
	fail_ci_if_error: true 
	verbose: true 
